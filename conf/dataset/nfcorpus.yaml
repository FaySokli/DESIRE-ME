data_dir: ../nfcorpus
corpus_path: ${dataset.data_dir}/nfcorpus_docs.jsonl
query_path: ${dataset.data_dir}/nfcorpus_queries.jsonl
qrels_path: ${dataset.data_dir}/train.binary.qrel
category_to_label: ${dataset.data_dir}/category_to_label.json
val_split: 0.1
output_dir: output/nfcorpus
model_dir: ${dataset.output_dir}/saved_models
logs_dir: ${dataset.output_dir}/logs
runs_dir: ${dataset.output_dir}/runs
